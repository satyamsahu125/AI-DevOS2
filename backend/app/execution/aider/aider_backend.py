from app.execution.backend import ExecutionBackend


class AiderBackend(ExecutionBackend):

    def execute(
        self,
        artifact,
        project,
    ):

        #
        # Future:
        # call Aider here
        #

        return artifact